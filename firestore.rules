rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Designs collection: Anyone can read, only authenticated users can write
    match /designs/{designId} {
      allow read: if true;
      allow write: if request.auth != null; 
    }
  }
}